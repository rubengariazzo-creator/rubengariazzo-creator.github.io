{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n eleventy,app -k \"eleventy --serve\" \"npm run watch --prefix app\"",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run clean && npm run build --prefix app && eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-img": "^5.0.0",
    "concurrently": "^9.0.0"
  },
  "overrides": {
    "sharp": "^0.35.4",
    "image-size": "^2.0.3"
  }
}
